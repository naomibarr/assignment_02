Write your reflection here.

See reflection.md for what makes a good reflection — be specific, use the
terminology from this assignment (package, module, ETL, coercion, public API,
relative import, accumulator, group by, unit vs. integration test), and make it
actionable.

I learned how a package can use modules to organize multiple parts of an ETL pipeline.
I struggled with coercion when writing clean_currency() and clean_quantity() to turn
the raw values into consistent numeric types. After using the testing panel and rereading
the instructions, I came to learn that the values must be stripped of special characters
before float() would accepct them. The unit tests helped me find problems with individual
functions, while the integration tests showed me when certain parts of my code were not 
working together correctly. Moving forward, I want to practice writing an accumulator on 
my own and use the unit tests more ofeten while I am writing the code instead of waiting 
until the end.